const Empty = () => (
  <div style={{
    display: 'grid',
    fontSize: '2rem',
    height: '100vh',
    placeItems: 'center'
  }}>
    <p>select a recipe from the list to get started!</p>
  </div>
)

export default Empty
